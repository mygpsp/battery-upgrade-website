# Google Cloud Function URL for order submission
# After deploying your Cloud Function, replace this with the actual URL
# Format: https://REGION-PROJECT_ID.cloudfunctions.net/submitOrder
NEXT_PUBLIC_ORDER_FUNCTION_URL=http://localhost:8080

# For local development, you can run the Cloud Function locally using:
# cd functions && npm start
